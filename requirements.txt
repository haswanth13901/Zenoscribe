# =============================================================================
# Python packages required to RUN the app in production.
#
#   pip install -r requirements.txt
#
# This is what the Dockerfile's production image installs - keep it limited
# to runtime dependencies only. Anything only needed for local development,
# testing, or linting belongs in requirements-dev.txt instead (which layers
# on top of this file via `-r requirements.txt`), not here - every package
# added here ships in the production image and widens its vulnerability
# surface.
# =============================================================================

# Web framework & server
fastapi==0.141.1
uvicorn[standard]==0.52.1
websockets==17.0.1
python-multipart==0.0.32
pydantic==2.13.4

# Database (Postgres) & migrations
psycopg[binary,pool]==3.2.10
alembic==1.19.1
# Pinned explicitly, not left to alembic's own transitive pin - alembic/env.py
# and every migration file import it directly (`sa.Column` etc.), and it runs
# on every app startup (db.init()), so an unpinned transitive bump could
# silently break migrations.
sqlalchemy==2.0.49

# Auth
bcrypt==5.0.0
PyJWT==2.13.0

# Config & HTTP (requests is used at runtime by soniox_client.py)
python-dotenv==1.2.2
requests==2.34.2
